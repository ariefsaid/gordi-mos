# 1Password coordinates for the production Resend SMTP key (NOT the secret itself).
# Fetched at deploy time via: op-get.sh "$OP_RESEND_ITEM" "$OP_RESEND_VAULT" "$OP_RESEND_FIELD"
# See supabase/README.md § Production email.
OP_RESEND_ITEM=resend-api-gordi-mos
OP_RESEND_VAULT=AS
OP_RESEND_FIELD=credential
