# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# Dependencies / build
node_modules
dist
dist-ssr
*.local

# macOS
.DS_Store

# Editor
.vscode/*
!.vscode/extensions.json
.idea
*.sw?

# Archives / backups
*.zip

# Test / coverage / e2e artifacts
coverage
playwright-report
test-results

# Vendored third-party Claude Code skills — re-create with scripts/vendor-skills.sh
.claude/skills/
# Codex-harness vendored skill mirror (same convention as .claude/skills/ — regenerated, not committed)
.agents/skills/

# agent worktrees (transient)
.claude/worktrees/

# Transient design-review artifacts (screenshots, Playwright-MCP dumps, audit scratch — regenerated per review)
.playwright-mcp/
/review/
/mos-*.png

# Secrets — NEVER commit (public repo). Repo-wide guard (app env lives in pmo-portal/.gitignore).
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
id_rsa*
*.keystore
**/serviceAccount*.json
secrets.*

# Supabase deploy-time real-people seed — NEVER commit (public repo, OD-P1-6).
# The .example template IS committed; the filled-in real file is not.
supabase/seed.production.sql

# Claude Code local (per-machine) settings — may contain personal tool allowlists
.claude/settings.local.json

# AGPL firewall — upstream Twenty CRM reference, never commit
/Twenty Design System/

# audit/agent runtime residue — never tracked (swept in once by a git add -A; audit F11)
.pi-subagents/
e11/

# session capture debris — never commit
.playwright-cli/
output/
mos-app/capture-*.mjs

# Operational runbooks: live host names, credential file paths, financial figures.
# Kept on disk, never tracked — a public repo must not carry a target list (2026-07-30).
docs/reference/warehouse-online.md
__pycache__/

# Docs — tracked in their OWN nested repo (docs/.git), NOT by this repo.
# THIS REPO IS PUBLIC (free GitHub Actions). Code syncs to GitHub; docs stay local.
# ADRs, decisions, runbooks, infra coordinates, and the archived history all live there.
# See the banner in CLAUDE.md before adding anything to the public tree.
docs/
