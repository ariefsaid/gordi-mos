# Screenshots note — design audit — 2026-07-08

Rendered audit screenshots were captured with `agent-browser` and saved locally at:

- `/tmp/mos-audit-2/shots/desktop/*.png`
- `/tmp/mos-audit-2/shots/phone/*.png`

Key overlays also captured:

- `deputy-panel.png`
- `search-panel.png`
- focus-ring spot checks under `focus-home-tab*.png`
