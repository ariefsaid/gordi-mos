{
  "schemaVersion": 2,
  "generatedAt": "2026-07-29T12:00:00Z",
  "title": "Design System: Gordi MOS",
  "extensions": {
    "colorMeta": {
      "primary": {
        "role": "primary",
        "displayName": "The One Blue",
        "canonical": "oklch(0.546 0.2153 262.8719)",
        "tonalRamp": [
          "oklch(0.20 0.09 262.87)",
          "oklch(0.29 0.13 262.87)",
          "oklch(0.38 0.17 262.87)",
          "oklch(0.47 0.20 262.87)",
          "oklch(0.546 0.2153 262.87)",
          "oklch(0.66 0.17 262.87)",
          "oklch(0.80 0.10 262.87)",
          "oklch(0.94 0.03 262.87)"
        ]
      },
      "background": {
        "role": "neutral",
        "displayName": "Warm White Canvas",
        "canonical": "oklch(0.994 0.002 85.0)",
        "tonalRamp": [
          "oklch(0.20 0.002 85)",
          "oklch(0.31 0.002 85)",
          "oklch(0.43 0.002 85)",
          "oklch(0.55 0.002 85)",
          "oklch(0.67 0.002 85)",
          "oklch(0.80 0.002 85)",
          "oklch(0.92 0.002 85)",
          "oklch(0.994 0.002 85)"
        ]
      },
      "foreground": {
        "role": "neutral",
        "displayName": "Warm Near-Black",
        "canonical": "oklch(0.145 0.004 30.0)",
        "tonalRamp": [
          "oklch(0.145 0.004 30)",
          "oklch(0.25 0.004 30)",
          "oklch(0.36 0.004 30)",
          "oklch(0.47 0.006 30)",
          "oklch(0.58 0.006 30)",
          "oklch(0.70 0.005 30)",
          "oklch(0.84 0.004 30)",
          "oklch(0.95 0.003 30)"
        ]
      },
      "muted-foreground": {
        "role": "neutral",
        "displayName": "Quiet Label Grey",
        "canonical": "oklch(0.388 0.012 30.0)",
        "tonalRamp": [
          "oklch(0.16 0.012 30)",
          "oklch(0.27 0.012 30)",
          "oklch(0.388 0.012 30)",
          "oklch(0.50 0.012 30)",
          "oklch(0.62 0.010 30)",
          "oklch(0.74 0.008 30)",
          "oklch(0.86 0.005 30)",
          "oklch(0.96 0.003 30)"
        ]
      },
      "border": {
        "role": "neutral",
        "displayName": "The Single Border",
        "canonical": "oklch(0.922 0.004 38.0)",
        "tonalRamp": [
          "oklch(0.22 0.004 38)",
          "oklch(0.34 0.004 38)",
          "oklch(0.46 0.004 38)",
          "oklch(0.58 0.004 38)",
          "oklch(0.70 0.004 38)",
          "oklch(0.82 0.004 38)",
          "oklch(0.922 0.004 38)",
          "oklch(0.97 0.003 38)"
        ]
      },
      "destructive": {
        "role": "tertiary",
        "displayName": "Signal Red",
        "canonical": "oklch(0.6368 0.2078 25.3259)",
        "tonalRamp": [
          "oklch(0.24 0.09 25.33)",
          "oklch(0.34 0.13 25.33)",
          "oklch(0.44 0.17 25.33)",
          "oklch(0.54 0.20 25.33)",
          "oklch(0.6368 0.2078 25.33)",
          "oklch(0.74 0.16 25.33)",
          "oklch(0.86 0.09 25.33)",
          "oklch(0.95 0.03 25.33)"
        ]
      },
      "warning": {
        "role": "tertiary",
        "displayName": "Aging Amber",
        "canonical": "oklch(0.8334 0.1641 83.8666)",
        "tonalRamp": [
          "oklch(0.30 0.07 83.87)",
          "oklch(0.42 0.10 83.87)",
          "oklch(0.53 0.13 83.87)",
          "oklch(0.64 0.15 83.87)",
          "oklch(0.74 0.16 83.87)",
          "oklch(0.8334 0.1641 83.87)",
          "oklch(0.91 0.10 83.87)",
          "oklch(0.97 0.04 83.87)"
        ]
      },
      "success": {
        "role": "tertiary",
        "displayName": "Settled Green",
        "canonical": "oklch(0.7205 0.192 149.4926)",
        "tonalRamp": [
          "oklch(0.26 0.08 149.49)",
          "oklch(0.37 0.11 149.49)",
          "oklch(0.47 0.14 149.49)",
          "oklch(0.57 0.17 149.49)",
          "oklch(0.66 0.19 149.49)",
          "oklch(0.7205 0.192 149.49)",
          "oklch(0.86 0.11 149.49)",
          "oklch(0.95 0.04 149.49)"
        ]
      },
      "brand-navy": {
        "role": "secondary",
        "displayName": "Structural Navy",
        "canonical": "oklch(0.3154 0.0639 260.7289)",
        "tonalRamp": [
          "oklch(0.18 0.04 260.73)",
          "oklch(0.3154 0.0639 260.73)",
          "oklch(0.42 0.08 260.73)",
          "oklch(0.53 0.09 260.73)",
          "oklch(0.64 0.08 260.73)",
          "oklch(0.76 0.06 260.73)",
          "oklch(0.87 0.03 260.73)",
          "oklch(0.96 0.01 260.73)"
        ]
      },
      "brand-orange": {
        "role": "secondary",
        "displayName": "Brand Sprinkle Orange",
        "canonical": "oklch(0.619 0.1833 39.9351)",
        "tonalRamp": [
          "oklch(0.24 0.08 39.94)",
          "oklch(0.34 0.12 39.94)",
          "oklch(0.44 0.15 39.94)",
          "oklch(0.53 0.17 39.94)",
          "oklch(0.619 0.1833 39.94)",
          "oklch(0.74 0.14 39.94)",
          "oklch(0.86 0.08 39.94)",
          "oklch(0.95 0.03 39.94)"
        ]
      },
      "violet": {
        "role": "tertiary",
        "displayName": "Categorical Violet",
        "canonical": "oklch(0.5424 0.2454 293.016)",
        "tonalRamp": [
          "oklch(0.20 0.10 293.02)",
          "oklch(0.29 0.15 293.02)",
          "oklch(0.38 0.19 293.02)",
          "oklch(0.46 0.22 293.02)",
          "oklch(0.5424 0.2454 293.02)",
          "oklch(0.68 0.19 293.02)",
          "oklch(0.82 0.11 293.02)",
          "oklch(0.94 0.04 293.02)"
        ]
      }
    },
    "typographyMeta": {
      "page-title": {
        "displayName": "Page Title",
        "purpose": "One per page, in .page-head. Plus Jakarta Sans 600/24px. The display scale STOPS here — OD-V4-8 deferred minting any step above it."
      },
      "heading": {
        "displayName": "Heading",
        "purpose": "Section and card headings."
      },
      "subheading": {
        "displayName": "Subheading",
        "purpose": "18px display. Reserved for a title that must OUTRANK the content beneath it: sub-section headers inside detail panels, empty-state titles, and a COMPACT page h1 (Home's day header steps page-title 24px -> subheading 18px to pay for its second full-width row — one rung down the same ladder, not a new size). A header that merely GROUPS PEERS does not qualify — that is body-lg. OD-V4-8 ratified the body rungs and the 24px display ceiling but never named this rung; recorded 2026-07-29."
      },
      "body-lg": {
        "displayName": "Emphasised Body",
        "purpose": "Record titles, primary row text, the v4 capture-row dish name, the Signal row title, summary-rule values, Home tile NAMES (.home-tile-name), and the v4 GROUP HEADERS (.stream-band-label, .signal-feed-label) — which sit at the same rung as the row titles they head and separate on display face + sentence case + full foreground + a hairline edge + asymmetric space, never on size. The 15/14 pair with body is a ratified rung (OD-REDESIGN-91 B4), not drift."
      },
      "body": {
        "displayName": "Body",
        "purpose": "Base body, table cells, meta. Equals the html base size."
      },
      "control": {
        "displayName": "Control",
        "purpose": "Buttons, inputs, selects, tabs — controls run 13.5px. NOTE: .btn base is 13.5px/600 but .btn-ghost deliberately steps to weight 500 (the quietest rank in the hierarchy)."
      },
      "label": {
        "displayName": "Label",
        "purpose": "Pill and field labels, dense metadata, the v4 summary-rule labels, Home tile COUNTS (.home-tile-count) and row status text. Home tile NAMES are body-lg, not this rung — at 12px inside 15px/600 rows the tile's own name was the quietest thing in the tile."
      },
      "overline": {
        "displayName": "Overline",
        "purpose": "Counts, column headers, captions, the v4 stream reason chip."
      },
      "mono": {
        "displayName": "Mono",
        "purpose": "SF Mono. Machine IDs and codes only — never a costume for 'technical'. The 13px NUMBER is also borrowed for sizing dense meta lines and segmented-track labels, which keep the DM Sans face (font-family: inherit)."
      }
    },
    "shadows": [
      {
        "name": "rest",
        "value": "0 1px 2px hsl(222 18% 12% / 0.05), 0 1px 3px hsl(222 18% 12% / 0.04)",
        "purpose": "The one resting lift. Cards, KPI tiles, kanban cards and Home bento tiles ONLY — co-signs the 1px border, never replaces it (OD-P3-11). Flat utility surfaces (toolbars, rows, strips, the v4 summary rule, the v4 List band, the v4 progress track) get none."
      },
      {
        "name": "state-lift",
        "value": "0 2px 10px hsl(240 6% 10% / 0.06)",
        "purpose": "Card or KPI tile on hover — deepens from rest."
      },
      {
        "name": "kanban-hover",
        "value": "0 4px 14px hsl(240 6% 10% / 0.10)",
        "purpose": "Draggable kanban card on hover; lifts more because it is grabbable."
      },
      {
        "name": "brand-button",
        "value": "0 1px 2px color-mix(in srgb, var(--primary) 25%, transparent)",
        "purpose": "Faint brand-tinted lift under the primary button."
      }
    ],
    "motion": [
      {
        "name": "state-transition",
        "value": "background 0.2s, border-color 0.2s, box-shadow 0.2s",
        "purpose": "Default control state easing. Motion here is a response to state, never decoration."
      }
    ],
    "measures": [
      {
        "name": "readable-column",
        "value": "1180px",
        "purpose": "The DEFAULT content measure (shell/page-families.css .page-frame__content). One column of prose-width content: records, forms, management surfaces, settings. Never widen a single-column page."
      },
      {
        "name": "wide-operating-surface",
        "value": "1760px",
        "purpose": "v4 (2026-07-29): the measure for a surface whose composition is genuinely two-dimensional — a master/detail split, or a work column beside a standing aside — where the readable cap leaves a DEAD GUTTER rather than a margin. TWO instances, deliberately the same number so their right edges land in the same place: Tasks master/detail (TasksWorkspace.css .split, 'kill the dead right void') and Home (home-layouts.css, scoped .page-frame--v3 .page-frame__content:has(.home-frame) — owner: 'why is the container for home got cut mid screen horizontally?'). A wide surface OPTS IN via :has() scoping and never widens the shared rule; a third instance must name the second dimension it composes and the void the readable cap leaves."
      },
      {
        "name": "form-measure",
        "value": "560px",
        "purpose": "A setting/profile card of short labelled fields (Identity, Language) — a form column, deliberately narrow. Declared as FORM_MEASURE in pages/profile-page.tsx; reused by the Café opening surface rather than minting a second value. This is an OUTER measure."
      },
      {
        "name": "picker-measure",
        "value": "720px content / 754px outer",
        "purpose": "A three-up wireframe chooser (the Home layout picker). 720px is the CONTENT measure the signed mockup drew it at on a bare div (#profile .setting), so the card host adds its chrome back on top: 720 + 2 x (16px padding + 1px border) = 754px outer, which is what ships. Taking a mockup's content measure as an outer measure silently narrows the content by 34px. At FORM_MEASURE the picker's cards measured 167px and the wireframes stopped being readable. Widening one host must not drag the other out — these are PER-CARD measures, never a page-level cap."
      }
    ],
    "breakpoints": [
      {
        "name": "home-bento-stack",
        "value": "620px",
        "purpose": "v4: the Home bento collapses to one column and every tile spans it — the tile grid is a desktop/tablet affordance (OD-V4-7 constraint 4). Measured on the Home frame (@container home), NOT the viewport."
      },
      {
        "name": "phone-toolbar",
        "value": "639px",
        "purpose": "v4: below this the Café toolbar keeps search + category on one line instead of wrapping."
      },
      {
        "name": "table-reflow",
        "value": "768px",
        "purpose": "DataTable switches between the desktop <table> and phone cards (OD-W4-4). Exactly one branch renders. The Home layout picker's 3-up thumbnail grid also drops to one column here — and stays 3-up all the way up: there is deliberately NO intermediate 2-up step. Measured: above this branch the picker's available content width never falls below 720px, and the narrowest point is the branch ITSELF (768px viewport, rail already collapsed, 24px frame gutters). It is not monotonic — the 920px icon-rail regime gives 800px and the 1100px full-rail regime 820px, both wider. At the 720px floor the card's inner width is 720 - 2 x (16px padding + 1px border) = 686px, so each thumbnail measures (686 - 2 x 12px gap) / 3 = ~221px, comfortably above the ~200px at which the CSS wireframes stop reading as diagrams. A 2-up branch would be a breakpoint with no measurement behind it."
      },
      {
        "name": "rail-collapse",
        "value": "920px",
        "purpose": "BELOW it the desktop rail is gone entirely and a hamburger appears; cmdk shrinks to an icon; user name/role hide. Distinct from table-reflow — do not conflate the two. AT OR ABOVE it the rail returns, but at 72px — see rail-compact."
      },
      {
        "name": "home-single-column",
        "value": "940px",
        "purpose": "v4: the Home work/feed split collapses — the Signals column stops being an aside and stacks under the work region (gap 32px to 24px), and the bento drops 6 columns to 4 (OD-V4-7). Measured on the Home frame (@container home), NOT the viewport."
      },
      {
        "name": "rail-compact",
        "value": "1100px",
        "purpose": "Between rail-collapse and this, the rail is ICON-ONLY at 72px (--rail-w-compact; OD-REDESIGN-84.2 / P1-1, the 920-1099.98px regime): the item keeps its icon, the label moves to .sr-only (still its accessible name) and a CSS-only tooltip surfaces it on hover/:focus-visible via attr(data-label); the top-bar brand column narrows to match and drops the wordmark so the divider stays on the rail boundary. At or above 1100px the full 232px rail returns. THIS REGIME IS WHY available content width is non-monotonic in the viewport across 768-1280."
      },
      {
        "name": "desktop",
        "value": "1280px",
        "purpose": "The full desktop contract: rail + header + frame + a 40-45% record panel, without clipping."
      }
    ],
    "containers": [
      {
        "name": "signal-feed",
        "value": "480px",
        "purpose": "v4: the Signal row branches on its OWN width, not the viewport — the same anatomy renders in Home's ~300px ambient tail and the archive Feed's ~1140px column at the same 1440px viewport, so a viewport media query measures the wrong thing — it left the 300px tail horizontal with a ~96px title that both wrapped and ellipsised. Declared as `container: signal-feed / inline-size`; below 480px the row stacks so the title takes the full column and the tail drops beneath it."
      },
      {
        "name": "home",
        "value": "940px / 620px",
        "purpose": "v4: declared `container: home / inline-size` on .home-frame (the Home page-content WRAPPER — a container query styles a container's DESCENDANTS, so .home-layout cannot be its own). Both Home branches read it instead of the window. Measured before the fix: the Overview work column ran 812px @1440 -> 488px @1100 -> 572px @1024, i.e. WIDER at the narrower window, because the 232px rail collapses at 920 independently of the viewport. At 1100 the bento was still holding 6 columns of 64.66px, squeezing row titles to 99px and wrapping meta to 3 lines. The thresholds are unchanged; only what they measure is. Diagnostic: if the quantity a breakpoint controls is not monotonic in the trigger, the trigger is the wrong quantity."
      }
    ],
    "knownGaps": [
      {
        "name": "no-spacing-token-set",
        "detail": "There is NO adopted semantic spacing token set. --space-* / --sp-* do not exist (zero declarations, zero uses). DESIGN.md's frontmatter `spacing:` block (xs/sm/md/base/lg/xl) has no runtime counterpart under those names. The platform layer DOES ship --ds-spacing-0..32 (+ -0_5, -1_5) on a 4px multiplicator in styles/tokens/theme-light.css / theme-dark.css, but it is effectively unadopted — a handful of declarations in two files (home-stream.css, signal-feed-section.css), all written with a raw-px fallback e.g. var(--ds-spacing-6, 24px). Everywhere else components write ON-SCALE RAW PX. Three half-systems, none authoritative. Binding rule until an owner decision closes it: any spacing value must land on the 4/8/12/16/20/24/32/48 scale, however it is written; do not mint a third vocabulary at a call site, and do not treat the scattered --ds-spacing-* uses as an adopted standard to spread."
      },
      {
        "name": "disabled-field-styling",
        "detail": "Disabled FIELD styling is still unratified in general (the Select component ratified its own: secondary bg + muted-foreground text + not-allowed cursor). Buttons DO have it shipped in ui/Button.css (opacity .5, not-allowed, no shadow, no background-image, saturate .35)."
      }
    ]
  },
  "components": [
    {
      "name": "Primary Button",
      "kind": "button",
      "refersTo": "button-primary",
      "description": "The one action affordance. The One Blue, kept under ~10% of any screen.",
      "html": "<button class=\"ds-btn-primary\">Submit</button>",
      "css": ".ds-btn-primary { height: 32px; padding: 0 12px; background: var(--primary); background-image: var(--gradient-primary-sheen); color: var(--primary-foreground); border: 0; border-radius: 8px; font-family: inherit; font-size: 13.5px; font-weight: 600; cursor: pointer; box-shadow: 0 1px 2px color-mix(in srgb, var(--primary) 25%, transparent); transition: background .2s, box-shadow .2s; } .ds-btn-primary:hover { background-image: none; background-color: color-mix(in srgb, var(--primary) 90%, transparent); } .ds-btn-primary:active { transform: translateY(1px); } .ds-btn-primary:focus-visible { outline: 2px solid var(--ring); outline-offset: 2px; } .ds-btn-primary:disabled { opacity: .5; cursor: not-allowed; box-shadow: none; background-image: none; filter: saturate(.35); }"
    },
    {
      "name": "Secondary Button",
      "kind": "button",
      "refersTo": "button-secondary",
      "description": "Bordered neutral action (.btn .btn-outline). Discard, Cancel, and every non-primary verb — and every door in an AMBIENT region (v4: Home's '+ Signal'), where the action blue would outrank the work the region exists to surface. Pinned by guard-signal-feed-row-fit.test.tsx.",
      "html": "<button class=\"ds-btn-secondary\">Discard</button>",
      "css": ".ds-btn-secondary { height: 32px; padding: 0 12px; background: var(--background); color: var(--foreground); border: 1px solid var(--input); border-radius: 8px; font-family: inherit; font-size: 13.5px; font-weight: 600; cursor: pointer; transition: background .2s; } .ds-btn-secondary:hover { background: var(--accent); } .ds-btn-secondary:focus-visible { outline: 2px solid var(--ring); outline-offset: 2px; }"
    },
    {
      "name": "Ghost Button",
      "kind": "button",
      "refersTo": "button-ghost",
      "description": "The quietest rank in the ONE button hierarchy (.btn .btn-ghost, ui/Button.css). NOTE the weight: the .btn base is 13.5px/600 but ghost deliberately steps DOWN to 500 — that is the variant's whole point. Recorded 2026-07-29 because both DESIGN.md and a code comment had written the canonical ghost as '13.5px/600'. It is 13.5px/500. A dead standalone .btn-ghost in tasks/TaskSurface.css was silently winning the cascade app-wide (measured on Home: 15px instead of 13.5px) purely because that file loads later; it was deleted (extract, 2026-07-28).",
      "html": "<button class=\"ds-btn-ghost\">Archive</button>",
      "css": ".ds-btn-ghost { height: 32px; padding: 0 12px; background: transparent; color: var(--muted-foreground); border: 0; border-radius: 8px; font-family: inherit; font-size: 13.5px; font-weight: 500; cursor: pointer; } .ds-btn-ghost:hover { background: var(--accent); color: var(--foreground); } .ds-btn-ghost:focus-visible { outline: 2px solid var(--ring); outline-offset: 2px; }"
    },
    {
      "name": "Search Input",
      "kind": "input",
      "refersTo": "input-search",
      "description": "The toolbar search-mini. Single border, 8px control radius, One-Blue focus ring.",
      "html": "<input class=\"ds-input-search\" type=\"search\" placeholder=\"Find a dish\" />",
      "css": ".ds-input-search { height: 36px; padding: 0 10px; background: var(--card); color: var(--foreground); border: 1px solid var(--border); border-radius: 8px; font-family: inherit; font-size: 13px; box-sizing: border-box; } .ds-input-search::placeholder { color: var(--muted-foreground); } .ds-input-search:focus { outline: 2px solid var(--ring); outline-offset: 2px; border-color: var(--ring); }"
    },
    {
      "name": "Status Pill",
      "kind": "chip",
      "refersTo": "pill-status",
      "description": "Dot plus tinted background and darkened text. The SHELL is an 8px rounded-rect (ratified OD-REDESIGN-91 #30/E1 — the 999px capsule is retired for the pill shell); only the dot stays circular. For SPARSE or exceptional status only — see Row Status Text when the status is on every row.",
      "html": "<span class=\"ds-pill ds-pill--destructive\"><span class=\"ds-pill-dot\"></span>Blocked</span>",
      "css": ".ds-pill { display: inline-flex; align-items: center; gap: 6px; padding: 3px 9px; border-radius: 8px; font-size: 12px; font-weight: 500; background: color-mix(in srgb, var(--destructive) 12%, transparent); color: var(--status-lost-text, var(--destructive)); } .ds-pill-dot { width: 6px; height: 6px; border-radius: 999px; background: currentColor; flex: none; }"
    },
    {
      "name": "Card",
      "kind": "card",
      "refersTo": "card-surface",
      "description": "Border AND the one resting shadow — co-equal, never shadow-alone (Soft-Elevation Rule).",
      "html": "<div class=\"ds-card\">Planned total<br><strong>548</strong></div>",
      "css": ".ds-card { background: var(--card); color: var(--card-foreground); border: 1px solid var(--border); border-radius: 12px; padding: 16px; box-shadow: 0 1px 2px hsl(222 18% 12% / 0.05), 0 1px 3px hsl(222 18% 12% / 0.04); transition: box-shadow .2s; } .ds-card:hover { box-shadow: 0 2px 10px hsl(240 6% 10% / 0.06); }"
    },
    {
      "name": "Rail Nav Item",
      "kind": "nav",
      "refersTo": "nav-rail-item",
      "description": "Destination row in the two-zone rail. 36px on a fine pointer (the ratified nav-item spec); raised to a 44px floor under (pointer: coarse). Active state is a quiet wash, not a blue fill. In the 920-1099.98px rail-compact regime the rail narrows to 72px and this row becomes icon-only: the label moves to .sr-only and a CSS-only tooltip surfaces it on hover/:focus-visible.",
      "html": "<a class=\"ds-nav-item ds-nav-item--active\" href=\"#\">Home</a>",
      "css": ".ds-nav-item { display: flex; align-items: center; gap: 10px; height: 36px; padding: 0 10px; border-radius: 8px; color: var(--foreground); text-decoration: none; font-size: 13.5px; } .ds-nav-item:hover { background: var(--secondary); } .ds-nav-item--active { background: var(--secondary); font-weight: 600; } .ds-nav-item:focus-visible { outline: 2px solid var(--ring); outline-offset: 2px; } @media (pointer: coarse) { .ds-nav-item { min-height: 44px; } }"
    },
    {
      "name": "Metric Summary Rule",
      "kind": "custom",
      "refersTo": "summary-rule",
      "description": "v4 signature. The derived-figure band for surfaces whose job is DOING, not reading. One line, no card, no shadow, hairline underneath. Replaces the KPI tile row on capture surfaces.",
      "html": "<section class=\"ds-summary-rule\"><span class=\"ds-metric\"><span class=\"ds-metric-label\">Planned total</span><span class=\"ds-metric-value\">548</span></span><span class=\"ds-metric\"><span class=\"ds-metric-label\">Made so far</span><span class=\"ds-metric-value\">421</span><span class=\"ds-metric-delta\">−127 vs plan</span></span><span class=\"ds-metric\"><span class=\"ds-metric-label\">% complete</span><span class=\"ds-metric-value\">77%</span></span></section>",
      "css": ".ds-summary-rule { display: flex; flex-wrap: wrap; align-items: baseline; column-gap: 22px; row-gap: 6px; padding-bottom: 10px; border-bottom: 1px solid var(--border); } .ds-metric { display: inline-flex; align-items: baseline; gap: 7px; } .ds-metric-label { font-size: 12px; color: var(--muted-foreground); white-space: nowrap; } .ds-metric-value { font-size: 15px; font-weight: 600; color: var(--foreground); font-variant-numeric: tabular-nums; } .ds-metric-delta { font-size: 12px; font-weight: 500; color: var(--destructive); white-space: nowrap; }"
    },
    {
      "name": "Thin Progress Track",
      "kind": "custom",
      "refersTo": "progress-track",
      "description": "v4 (2026-07-29). The one-line completion track — shipped on Home's day header ('N of M done'), and the shape any future AMBIENT progress mark takes. NOT a new vocabulary: track = secondary, fill = success is the system's documented progress-bar pairing (the Data Table win-% bar), with the pill radius on BOTH so the fill's leading edge is never a square corner inside a rounded track. Height 4px is the only fixed rendered dimension and is on the geometry scale. `flex: 1 1 90px; max-width: 200px` are OFF the 4/8/12/16/20/24/32/48 scale and are SANCTIONED, not drift: the geometry scale governs FIXED dimensions (padding, gaps, heights, radii — anything that lands as a rendered edge); a flex-basis and a max-width on a FLEXIBLE track are the bracket inside which the flex algorithm picks the width, and the rendered width is essentially never either number. No token is minted — a primitive two instances away carrying two magic numbers is worse than a documented recipe. The BOUNDS are the rule: a floor wide enough to read as a proportion (~90px), a ceiling that stops it becoming the widest mark in the header (~200px). A third consumer is the trigger to promote it. Never the sole carrier — it always sits beside the state line that says the same thing in words (WCAG 1.4.1). Flat at rest.",
      "html": "<div class=\"ds-hdr-state\"><span class=\"ds-hdr-msg\">3 of 11 done — 2 overdue</span><span class=\"ds-track\"><span class=\"ds-track-fill\" style=\"width:27%\"></span></span></div>",
      "css": ".ds-hdr-state { display: flex; align-items: center; gap: 12px; flex-wrap: wrap; } .ds-track { flex: 1 1 90px; max-width: 200px; height: 4px; background: var(--secondary); border-radius: var(--radius-pill); overflow: hidden; } .ds-track-fill { display: block; height: 100%; background: var(--success); border-radius: var(--radius-pill); }"
    },
    {
      "name": "Compact Capture Row",
      "kind": "custom",
      "refersTo": "capture-row",
      "description": "v4 signature. The phone row for running a long list and acting on each item. Identity left, TYPED numeric field right at the thumb, meta line beneath that renders only when it has something to say. ~66px against the generic record card's ~200px. The control is a typed field, NOT a stepper: amounts here are 10-20+, so -/+ meant ~20 taps per row (owner-corrected, DD-5). font-size is 16px so mobile Safari does not zoom the viewport on focus. The 10px 12px padding rhythm lives in dashboard/data-table.css beside the .dt-card it modifies — PhoneCard (that same file's component) applies .dt-card--compact whenever a renderCard seam is supplied, and it has four consumers (Café Log/Plan/Pushes/Review). It previously lived in the page-specific, code-split pages/kitchen-log-page.css: live-measured on a fresh Café Plan load with that chunk never fetched, computed padding was 14px (the un-compacted .dt-card fallback). A class the shared primitive applies belongs with that primitive.",
      "html": "<div class=\"ds-capture-row\"><div class=\"ds-capture-head\"><span class=\"ds-capture-name\">Ayam Garang Asem</span><span class=\"ds-qty-wrap\"><input class=\"ds-qty\" type=\"number\" inputmode=\"decimal\" enterkeyhint=\"next\" placeholder=\"25\" aria-label=\"Quantity produced for Ayam Garang Asem\" /><span class=\"ds-qty-unit\">porsi</span></span></div><div class=\"ds-capture-meta\"><span class=\"ds-capture-status\">Under −7</span></div></div>",
      "css": ".ds-capture-row { display: flex; flex-direction: column; gap: 6px; padding: 10px 12px; background: var(--card); border: 1px solid var(--border); border-radius: 12px; } .ds-capture-head { display: flex; align-items: flex-start; justify-content: space-between; gap: 12px; } .ds-capture-name { font-size: 15px; font-weight: 600; color: var(--foreground); overflow-wrap: anywhere; } .ds-capture-meta { display: flex; flex-wrap: wrap; align-items: baseline; gap: 4px 10px; font-size: 12px; color: var(--muted-foreground); } .ds-capture-status { color: var(--status-lost-text); font-weight: 500; } .ds-qty-wrap { display: inline-flex; align-items: center; gap: 6px; flex: none; } .ds-qty { width: 68px; height: 44px; box-sizing: border-box; text-align: right; padding-right: 8px; border: 1px solid var(--border); border-radius: 8px; background: var(--background); color: var(--foreground); font-size: 16px; font-weight: 600; font-variant-numeric: tabular-nums; font-family: inherit; } .ds-qty::placeholder { color: var(--muted-foreground); } .ds-qty:focus-visible { outline: 2px solid var(--ring); outline-offset: 2px; } .ds-qty-unit { font-size: 12px; color: var(--muted-foreground); }"
    },
    {
      "name": "List Band",
      "kind": "custom",
      "refersTo": "list-band",
      "description": "v4 signature (2026-07-29). A HEADED GROUP OF ROWS — the List arrangement's answer to the bento tile, and deliberately NOT a card. Three cues and nothing else: (a) a 1px border HAIRLINE EDGE on top, the group's boundary — without it the label floated in the column with the same 24px of air above it as any other gap and four regions read as one undifferentiated list (owner: 'i dont like the header on the current wall of text', 2026-07-28); (b) a DISPLAY-FACE header at body-lg/600 in full foreground, sentence case — the SAME rung as the row titles it heads, because at 18px it ties Home's own h1 and the two read as peers; (c) ASYMMETRIC SPACE — 24px above the rule, 12px rule-to-label, 6px label-to-rows. The grouping cue IS the asymmetry: space that decreases as you descend binds the label to what it heads. A symmetric band is just another gap. No box, no fill, no shadow — a band that grows a card shell is a nested card, which OD-V4-7 constraint 3 forbids one level up. No decorative hue: amber/red/blue already mean overdue/blocked/in-progress on these very rows. Pinned by guard-list-band-separation.css.test.ts.",
      "html": "<section class=\"ds-band\"><div class=\"ds-band-head\"><h2 class=\"ds-band-label\">Needs you now</h2><a class=\"ds-band-link\" href=\"#\">All 12 →</a></div><ul class=\"ds-band-list\"><li class=\"ds-band-row\">Kirim revisi RAB Gedung B</li><li class=\"ds-band-row\">Approve PO kopi Arabika</li></ul><p class=\"ds-band-more\">+3 more</p></section>",
      "css": ".ds-band { display: flex; flex-direction: column; min-width: 0; border-top: 1px solid var(--border); padding-top: 12px; } .ds-band-head { display: flex; align-items: baseline; gap: 12px; margin-bottom: 6px; } .ds-band-label { flex: 1; min-width: 0; margin: 0; font-family: var(--font-display); font-size: 15px; font-weight: 600; color: var(--foreground); } .ds-band-link { flex: none; font-size: 13px; font-weight: 600; color: var(--primary); text-decoration: none; white-space: nowrap; } .ds-band-list { list-style: none; margin: 0; padding: 0; display: flex; flex-direction: column; } .ds-band-row + .ds-band-row { border-top: 1px solid var(--border); } .ds-band-more { margin: 0; padding: 6px 4px 0; font-size: 12px; color: var(--muted-foreground); }"
    },
    {
      "name": "Home Bento Tile",
      "kind": "custom",
      "refersTo": "home-tile",
      "description": "v4 signature (OD-V4-7). The Overview arrangement's tile: the CARD recipe unchanged (same surface, border, 12px radius, 16px padding, one resting lift) placed in a 6-column grid whose gap (20px) deliberately EXCEEDS the tile's own padding (16px) — separation between groups must beat spacing within them or the grid reads as one mush. Exactly TWO weights exist and they sum to the track: wide spans 4, narrow spans 2, so 6 columns pack 4+2 then 2+4 and 4 columns pack 4 | 2+2 | 4. A third weight would leave a hole beside a tile at one band or the other. Every tile carries real rows, never icon + heading + stat, and is one level deep — no nested cards. The tile NAME is body-lg (15px) display face, NOT the label rung: at 12px inside 15px/600 rows the tile's own name was the quietest thing in the tile. The LEAD tile is marked by TONE keyed to its REGION (--surface-tertiary), never by weight — needs-you and my-work are both wide, so a weight-keyed fill would raise both and mark neither; it is a different step of the ramp from the row-hover fill (secondary) so a lifted tile never reads as hovered. Both branches are @container queries on the Home frame, not viewport media.",
      "html": "<div class=\"ds-bento\"><section class=\"ds-tile\" data-weight=\"wide\" data-region=\"needs-you\"><div class=\"ds-tile-head\"><h2 class=\"ds-tile-name\">Needs you</h2><span class=\"ds-tile-count\">7</span></div><ul class=\"ds-tile-rows\"><li>Kirim revisi RAB Gedung B<span>Overdue · 8d</span></li><li>Approve PO kopi Arabika<span>Due today</span></li></ul></section><section class=\"ds-tile\" data-weight=\"narrow\"><div class=\"ds-tile-head\"><h2 class=\"ds-tile-name\">Mentions</h2><span class=\"ds-tile-count\">2</span></div><ul class=\"ds-tile-rows\"><li>Riri di Café · stok susu<span></span></li></ul></section></div>",
      "css": ".ds-bento { display: grid; grid-template-columns: repeat(6, minmax(0, 1fr)); gap: 20px; align-items: stretch; } .ds-tile { display: flex; flex-direction: column; min-width: 0; padding: 16px; background: var(--card); border: 1px solid var(--border); border-radius: 12px; box-shadow: 0 1px 2px hsl(222 18% 12% / 0.05), 0 1px 3px hsl(222 18% 12% / 0.04); } .ds-tile[data-weight='wide'] { grid-column: span 4; } .ds-tile[data-weight='narrow'] { grid-column: span 2; } .ds-tile[data-region='needs-you'] { background: var(--surface-tertiary); } .ds-tile-head { display: flex; align-items: baseline; justify-content: space-between; gap: 8px; margin-bottom: 8px; } .ds-tile-name { margin: 0; font-family: var(--font-display); font-size: 15px; font-weight: 600; color: var(--foreground); } .ds-tile-count { font-size: 12px; color: var(--muted-foreground); white-space: nowrap; } .ds-tile-rows { list-style: none; margin: 0; padding: 0; display: flex; flex-direction: column; } .ds-tile-rows li { display: flex; align-items: baseline; justify-content: space-between; gap: 12px; padding: 7px 0; font-size: 15px; font-weight: 600; color: var(--foreground); } .ds-tile-rows li + li { border-top: 1px solid var(--border); } .ds-tile-rows li span { font-size: 12px; font-weight: 500; color: var(--muted-foreground); white-space: nowrap; } @container home (max-width: 940px) { .ds-bento { grid-template-columns: repeat(4, minmax(0, 1fr)); } } @container home (max-width: 620px) { .ds-bento { grid-template-columns: minmax(0, 1fr); } .ds-tile { grid-column: span 1 !important; } }"
    },
    {
      "name": "Segmented Track",
      "kind": "custom",
      "refersTo": "segmented-track",
      "description": "extract (2026-07-28): promoted from two pixel-identical re-authorings (Money's Branch/Activity CutToggle and Home's order-preference toggle). One visual grammar, TWO ARIA shapes on top of it — a role=tablist view-switcher (aria-selected state) and a role=radiogroup persistent setting (.is-active state) — because the interaction contracts genuinely differ even though the paint doesn't. Canonical CSS: src/styles/segmented-track.css).home-order-seg-opt selectors still in the shared file are REMOVAL DEBT, not a live pattern. The shape stays documented because it is the correct contract if a persistent segmented setting returns. Geometry, live-measured: a 32px track with 3px inset padding, so each option renders at 26px (NOT the 28px previously stated). Label size borrows the mono token's 13px NUMBER for sizing only — the face stays DM Sans via font-family: inherit.  CURRENT STATE (reconciled 2026-08-06 against dev's DESIGN.md, which is ahead of the v4 copy this file was ported from): the .home-order-seg / .home-order-seg-opt selectors are GONE and no removal debt remains — guard-od-v4-10-retirement.test.ts scans src/ to keep them gone. There are TWO live consumers and ONE importer: dashboard/cut-toggle.css @imports the shared file, while dashboard/window-selector.css re-authors the same grammar pixel-for-pixel with no @import and renders directly beside it on Money's toolbar (open debt, issue #283). 'Every consumer imports it' is the intent, not the current state.",
      "html": "<div class=\"ds-seg\" role=\"tablist\"><button class=\"ds-seg-opt\" role=\"tab\" aria-selected=\"true\">Branch</button><button class=\"ds-seg-opt\" role=\"tab\" aria-selected=\"false\">Activity</button></div>",
      "css": ".ds-seg { display: inline-flex; gap: 4px; height: 32px; padding: 3px; background: var(--secondary); border-radius: 8px; box-sizing: border-box; } .ds-seg-opt { display: inline-flex; align-items: center; justify-content: center; height: 100%; padding: 0 12px; border: none; border-radius: calc(8px - 2px); background: transparent; color: var(--muted-foreground); font-family: inherit; font-size: 13px; font-weight: 600; cursor: pointer; } .ds-seg-opt[aria-selected='true'] { background: var(--background); color: var(--foreground); box-shadow: 0 1px 2px color-mix(in srgb, var(--ds-font-color-primary) 12%, transparent); cursor: default; } @media (max-width: 767.98px) { .ds-seg, .ds-seg-opt { height: auto; min-height: 44px; } }"
    },
    {
      "name": "Setting Card",
      "kind": "card",
      "refersTo": "setting-card",
      "description": "v4 (2026-07-29). A profile/setting card is sized by WHAT IT HOSTS, and there are two hosts. FORM_MEASURE (560px, outer) for short labelled fields — a form column, deliberately narrow. PICKER_MEASURE for a three-up wireframe chooser: 720px is the CONTENT measure the signed mockup drew it at on a bare div, so the host adds its chrome back on top — 720 + 2 x (16px padding + 1px border) = 754px OUTER, which is what ships. Taking a mockup's content measure as an outer measure silently narrows the content by 34px. At FORM_MEASURE the picker's cards measured 167px and the wireframes stopped being readable, which is the entire point of a diagram-based chooser. Widening one host must not drag the other out with it — these are per-card measures, never a page-level cap. Pinned by pages/profile-page.test.tsx FR-920.",
      "html": "<section class=\"ds-setting ds-setting--picker\"><h2>Home layout</h2><p class=\"ds-setting-help\">Choose how your Home page arranges its regions.</p></section>",
      "css": ".ds-setting { background: var(--card); color: var(--card-foreground); border: 1px solid var(--border); border-radius: 12px; padding: 16px; box-shadow: 0 1px 2px hsl(222 18% 12% / 0.05), 0 1px 3px hsl(222 18% 12% / 0.04); max-width: 560px; } .ds-setting--picker { max-width: 754px; } .ds-setting-help { color: var(--muted-foreground); font-size: 12px; margin: 0 0 12px; max-width: 52ch; }"
    },
    {
      "name": "Empty State",
      "kind": "custom",
      "refersTo": "empty-state",
      "description": "Four sanctioned archetypes, matching EmptyStateVariant in components/ui/state-kit.tsx. quiet — calm nothing-to-do; icon + title + one-line reason, NO cta (e.g. /inbox caught up). next-step — one direct action resolves it; icon + title + explanation + ONE action. awaiting — refills from submissions or a sync-fed source; adds a muted retry/status line + one refresh action. blank — empty BY DESIGN (a placeholder route, an unbuilt slice); deliberately the quietest, with no accent tint, so it never reads as an earned all-clear or as pending work. Rule: one route, one empty frame, one action maximum; the archetypes vary only the message and whether the single action slot is filled. Spacing, type, icon treatment and body measure stay shared, and no new empty-state colours, fonts or chrome are minted. ADDED 2026-08-06 — absent from the v4 copy this file was ported from; `blank` postdates v4 (CardHead.css records it)."
    }
  ],
  "narrative": {
    "northStar": "The Quiet Control Surface",
    "overview": "A near-monochrome system whose only saturated colour in normal use is the primary blue. Status colours appear only on data state, and a single categorical violet is reserved for KPI and timeline accents — never as an action colour. Structure comes from one 1px border and surface-tone contrast, plus one subtle resting lift on cards. The system is calm, dense and data-first: it is meant to be looked at all day by people who came to get something done, on a café floor or at a desk.",
    "keyCharacteristics": [
      "One blue for every interactive affordance, under ~10% of any screen",
      "One border value; structure by line and tone, not by shadow",
      "Exactly one resting shadow, on cards/KPI/kanban only",
      "Tabular figures on every number that can be compared",
      "Density is a feature — usability and speed beat model completeness",
      "Colour is rationed to what needs action",
      "Two content measures: 1180px readable column, 1760px wide operating surface"
    ],
    "rules": [
      {
        "name": "The One Blue Rule",
        "body": "Every interactive affordance uses the one primary blue, kept under ~10% of any screen. The primary-button sheen is the same blue, not a second action colour.",
        "section": "colors"
      },
      {
        "name": "The Single-Border Rule",
        "body": "border equals input. One border value defines all structure.",
        "section": "colors"
      },
      {
        "name": "The Tabular-Numbers Rule",
        "body": "Every figure that can change or be compared is tabular-nums. DM Sans tnum is a verified no-op in its fontsource build, so the .tabular utility is scoped to Inter Variable; proportional body text stays DM Sans.",
        "section": "typography"
      },
      {
        "name": "The Two-Role Scale Rule",
        "body": "The scale has exactly two roles and they do not meet: display runs 24/20/18px in Plus Jakarta Sans, body runs 15/14/13.5/12/11px in DM Sans. The display scale STOPS at 24px — no step above page-title exists and OD-V4-8 deferred minting one, so emphasis above the page title comes from position, weight and air, never a new primitive. Inside the body role the 15/14 pair (body-lg / body) is a deliberate rung (OD-REDESIGN-91 B4): a linter flagging it as a flat type hierarchy is reading a two-role scale as one ramp. Known non-defect.",
        "section": "typography"
      },
      {
        "name": "The 18px Rung Rule",
        "body": "OD-V4-8 ratified the body rungs (15/14/13.5/12/11) and the 24px display ceiling but never named the 18px subheading rung, which two shipped Home decisions turn on. (1) A COMPACT PAGE H1 steps to subheading (18px) — one rung below page-title, not three: where a head carries a second full-width row, the block's ~70px budget is paid for by stepping down the SAME declared ladder. It landed first at body-lg, which put the page's own h1 BELOW the group headers inside it. (2) A GROUP HEADER sits at body-lg (15px), the SAME rung as the record-row titles it heads, and separates on four other channels: the display face, sentence case, full foreground, a 1px border hairline edge above, and asymmetric space. Size is deliberately not one of the channels — at 18px the label ties Home's own h1 and the two read as peers. Therefore subheading stays RESERVED for a title that must OUTRANK the content beneath it (detail-panel sub-sections, empty-state titles, the compact page h1); a header that merely groups peers does not qualify. Neither carries a decorative hue — amber/red/blue are the row status vocabulary. Recorded 2026-07-29; pinned by guard-list-band-separation.css.test.ts and guard-home-head-rank.css.test.ts.",
        "section": "typography"
      },
      {
        "name": "The Two-Measure Rule",
        "body": "There are TWO content measures and which one a page gets is decided by what kind of surface it is, not by who authored it. 1180px is the READABLE SINGLE COLUMN and the default (page-families.css) — records, forms, management surfaces, settings. 1760px is the WIDE OPERATING SURFACE measure, for a surface whose composition is genuinely two-dimensional (a master/detail split, or a work column beside a standing aside) where the readable cap leaves a DEAD GUTTER rather than a margin. It is a rule with TWO instances, deliberately the same number so their right edges land in the same place: Tasks master/detail (TasksWorkspace.css .split) and Home (home-layouts.css, :has(.home-frame)-scoped). A wide surface OPTS IN by specificity and never widens the shared rule; a third instance must name the second dimension it composes and the void the readable cap leaves at a real window width. Never widen a single-column page (v4, 2026-07-29).",
        "section": "layout"
      },
      {
        "name": "The Soft-Elevation Rule",
        "body": "Cards, KPI tiles and kanban cards carry exactly one subtle resting shadow in addition to their 1px border. Every other surface stays flat at rest. Never stack resting shadows; never exceed ~0.06 total alpha (OD-P3-11).",
        "section": "elevation"
      },
      {
        "name": "The Restrained-Gradient Rule",
        "body": "Only the two ratified navy-tinted whispers: the primary-button sheen and the home/digest surface wash. Never purple (OD-P3-12).",
        "section": "colors"
      },
      {
        "name": "The Container-Query Rule",
        "body": "A component that renders at different widths inside the same viewport adapts to the space it has, not the window. The Signal row renders in Home's ~300px ambient tail AND the /work/signals archive Feed's ~1140px column at the same 1440px viewport, so a viewport media query measures the wrong thing — it left the 300px tail horizontal with a ~96px title that both wrapped and ellipsised. Home is the second case and shows the failure mode: its branches keyed off the window while the 232px rail collapses at 920 and narrows to 72px between 920 and 1100 INDEPENDENTLY of it, so the space the arrangements actually had was non-monotonic in the viewport (work column 812px @1440, 488px @1100, 572px @1024). Declare a container and branch with @container. Reach for @media only when the thing that actually varies is the viewport. Diagnostic: if the quantity a breakpoint controls is not monotonic in the trigger, the trigger is the wrong quantity (v4).",
        "section": "layout"
      },
      {
        "name": "The Full-Row Packing Rule",
        "body": "Tile weights exist so consecutive tiles pack to exactly the grid's column count at every desktop band — never so a tile is left with a hole beside it. There are exactly two weights and they sum to the track: wide spans 4, narrow spans 2, giving 4+2 then 2+4 at six columns and 4 | 2+2 | 4 at four. Adding a third weight is not a styling choice; it breaks the packing invariant at one band or the other (v4, OD-V4-7).",
        "section": "components"
      },
      {
        "name": "The Surface-Job Rule",
        "body": "Pick the metric treatment from the surface's job: the summary rule where the user came to act, KPI tiles where they came to read. A capture surface never opens with a row of tiles (v4).",
        "section": "components"
      },
      {
        "name": "The No-Restated-Value Rule",
        "body": "Never repeat a value under a control that the row or card already renders as its own column or field (v4).",
        "section": "components"
      },
      {
        "name": "The One-Definition Rule",
        "body": "A CSS class name is global regardless of which component file declares it, so exactly one file may define any given selector. A second, differently-valued definition of the same class elsewhere in the app is not a harmless local override — it silently wins or loses the cascade by import order, not intent (extract, 2026-07-28: found live on four separate classes — .sr-only in four page/component files, and a dead .btn-ghost that was shadowing the canonical Button ghost variant app-wide, measured on Home at 15px instead of the canonical 13.5px). Shared visual grammar used by more than one component belongs in one file the consumers @import, not re-authored per surface. Corollary (2026-07-28): a class the shared PRIMITIVE applies belongs with that primitive, not with whichever page authored it first — .dt-card--compact lived in a route-code-split page stylesheet and silently fell back to 14px on the three other pages that render it.",
        "section": "components"
      },
      {
        "name": "The Off-Scale Boundary",
        "body": "The 4/8/12/16/20/24/32/48 geometry scale governs FIXED dimensions — padding, gaps, heights, radii, anything that lands as a rendered edge. A flex-basis or a max-width on a FLEXIBLE track is neither: it is the bracket inside which the flex algorithm picks the width, and the rendered width is essentially never either number. That is the whole reason the v4 progress track's `flex: 1 1 90px; max-width: 200px` is sanctioned while its `height: 4px` had to be on-scale. This is a narrow exemption, not a general escape hatch: if the number lands as an edge the reader can see, it is on the scale (v4, 2026-07-29).",
        "section": "layout"
      }
    ],
    "dos": [
      "Do drive every interactive affordance with the one primary blue, and keep it under ~10% of any screen.",
      "Do define structure with the single 1px border and surface-tone contrast; cards also carry the one subtle resting lift.",
      "Do apply tabular-nums to every figure in tables, KPIs, kanban and funnels.",
      "Do show status as a 6px dot plus a tinted pill, and reserve solid fills for the destructive button only.",
      "Do keep controls at 32px with the 8px control radius; cards and overlays take the 12px card radius.",
      "Do set headings in Plus Jakarta Sans 600 and body/UI/table text in DM Sans; SF Mono for machine IDs only.",
      "Do expose the global focus-visible ring on every focusable element.",
      "Do reserve categorical violet and the status hues for non-interactive meaning.",
      "Do pick the metric treatment from the surface's job: the summary rule where the user came to act, KPI tiles where they came to read.",
      "Do drop a status pill's fill to toned text when the status is present on every row at rest.",
      "Do pack a tile grid to exactly its column count — two weights that sum to the track, never a third that leaves a hole beside a tile.",
      "Do branch on a container query when what actually varies is the width of the component's own container rather than the viewport.",
      "Do give a two-dimensional operating surface the 1760px wide measure and scope it with :has() so every readable page keeps 1180px.",
      "Do separate a group header from the rows it heads with the display face, sentence case, full foreground, a hairline edge and asymmetric space — not with a bigger font.",
      "Do size a setting card by what it hosts, and add the card's own padding and border back on top of a mockup's CONTENT measure.",
      "Do land every spacing value on the 4/8/12/16/20/24/32/48 scale, however it is written — there is no adopted spacing token set to reach for."
    ],
    "donts": [
      "Don't ship the AI-SaaS marketing aesthetic: no neon accents, no glassmorphism, no oversized hero type, no floating-card soup, no purple gradients.",
      "Don't stack resting shadows or give a flat utility surface a resting shadow.",
      "Don't use high-opacity black for shadows.",
      "Don't introduce a second action colour, a fourth typeface, a new gradient hue, or a new border value.",
      "Don't use mono or proportional figures for money in tables.",
      "Don't colour body text with a fully saturated status hue or fill a status pill solid.",
      "Don't invent radii outside the 4/8/10/12/999 scale.",
      "Don't mint a display step above 24px to make something outweigh the page title, and don't 'correct' the ratified 15/14px body pair.",
      "Don't take `subheading` (18px) for a header that merely groups peers — it is reserved for a title that must outrank the content beneath it.",
      "Don't paint an ambient affordance in the one action blue. A door in a side rail or feed tail takes the outline variant; the blue belongs to the surface's own primary action.",
      "Don't open a capture surface with a row of KPI tiles.",
      "Don't repeat a value under a control that the row or card already renders as its own column or field.",
      "Don't ship the generic record card on a surface whose phone job is running a long capture list.",
      "Don't widen a readable single-column page, and don't introduce a THIRD wide measure — the two wide surfaces share 1760px so their right edges agree.",
      "Don't add a responsive branch without the measurement that justifies it — the picker stays 3-up above 768px because its narrowest thumbnail there is ~221px, and content width is NOT monotonic in the viewport.",
      "Don't give a List band a box, fill or shadow — a band that grows a card shell is a nested card, which OD-V4-7 constraint 3 forbids one level up.",
      "Don't mint a new --space-* / --sp-* vocabulary at a call site, and don't spread the scattered --ds-spacing-* uses as if they were an adopted standard."
    ]
  }
}
